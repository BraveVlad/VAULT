import { StyleSheet, Text, View } from "react-native";
import React from "react";

type GamePageViewProps = {
	gameId: string;
};

export default function GamePageView() {
	return (
		<View>
			<Text>GamePageView</Text>
		</View>
	);
}
const styles = StyleSheet.create({});
